#!/bin/bash
export PATH="$HOME/.nix-profile/bin:$PATH"

# If no file arguments given, use default working directory
if [ $# -eq 0 ]; then
  cd "$HOME/src/github.com/bobstrange/memot/work/spideraf"
fi

exec nixGLIntel neovide "$@"
